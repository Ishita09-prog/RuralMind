# Screenshots

Save your app screenshots in this folder with these exact names so the main
README displays them:

- `chat.png` — the chat view (question + answer + buttons)
- `diagram.png` — a real labeled diagram result
- `dashboard.png` — the Progress dashboard (streak + badges)

You can drag-and-drop the images straight into this folder.
