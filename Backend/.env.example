# Copy this file to ".env" and fill in your own values.
# Used by the Diagram feature (Google Custom Search). Optional — if left blank,
# diagrams fall back to Wikimedia Commons, which needs no key.
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_CX=your_search_engine_id_here
